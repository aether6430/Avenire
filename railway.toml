[build]
buildCommand = "if [ \"$APP_ROLE\" = \"web\" ]; then pnpm --filter @avenire/web build; else pnpm --filter @avenire/backend build; fi"

[deploy]
startCommand = "if [ \"$APP_ROLE\" = \"web\" ]; then pnpm --filter @avenire/web start; else pnpm --filter @avenire/backend start:ingestion; fi"
